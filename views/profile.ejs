<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="style.css">
  <title>Perfil</title>
</head>
<body class="<%= themeColor %>">
  <%- include('./partials/header') %>
  <main>
    <section>
      <p><a href="/">< Volver al inicio</a></p>
    </section>
    <h3>Perfil</h3>
    <section>
      <p>Usuario: <strong><%= user %></strong></p><br>
      <form action="/logout" method="post">
        <button>Cerrar sesión</button>
      </form>
    </section>

    <h4>Carrito Espiritual</h4>
    <section>
      <% if (cart.length > 0) { %>
        <ul>
          <% cart.forEach(session => { %>
            <li><%= session.name %> - <%= session.price %>€</li>
          <% }) %>
        </ul>
        <p><strong>Total: <%= cart.reduce((sum, s) => sum + s.price, 0) %>€</strong></p>
        <form action="/cart/clear" method="post">
          <button type="submit">Vaciar carrito</button>
        </form>
      <% } else { %>
        <p>Tu carrito está vacío. <a href="/sessions">Ver sesiones disponibles</a></p>
      <% } %>
    </section>
  </main>
  <%- include('./partials/footer') %>
</body>
</html>